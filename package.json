{
  "name": "imf-africa-pay",
  "version": "1.0.0",
  "description": "IMF Africa Pay - Frontend and Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server.js",
    "client": "cd src && npm run dev",
    "build": "node build-frontend.js",
    "frontend-build": "cd src && npm install && npm run build",
    "static-build": "cd src && npm install && npm run build",
    "local-email": "node local-email-service.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "mongoose": "^7.5.0",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}